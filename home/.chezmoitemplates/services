{{- $boolean := includeTemplate "boolean" . | fromYaml -}}
services:
  system:
    {{ if lookPath "sddm" -}}
    - sddm
    {{ end -}}
    - bluetooth
  user:
    {{ if $boolean.desktop -}}
    - hyprsunset
    {{ end -}}
    - hyprpolkitagent
    - hypridle
    - waybar
    - swaync
    - hyda-devices
    - hyda-events
    {{- if $boolean.laptop }}
    - hyda-battery
    {{- end }}
